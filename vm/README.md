# Implementación de la Macchina en C.

Necesita que esté instalada la librería con el recolector de basura
conservador `gc` (en Ubuntu es el paquete `libgc-dev`).

Para compilar usar
```code
 make
```
